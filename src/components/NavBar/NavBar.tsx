import { SearchBox } from '../SearchBox/SearchBox'
import { FilterList } from '../FilterList/FilterList'

export const NavBar = () => {
  return (
    <section>
        <SearchBox/>
        <FilterList/>
    </section>
  )
}
